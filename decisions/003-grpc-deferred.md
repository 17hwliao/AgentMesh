# 决策 003：暂不引入 gRPC

**状态：** 已采用（当前 V0）

两个业务客户端目前通过 loopback HTTP/SSE 调用网关，既能验证流式转发、认证和取消，也避免同时维护 HTTP 与 gRPC 两套协议、鉴权和错误语义。

只有 AgentRunner 被拆为独立进程且确实需要强类型同步调用、进度流与跨进程取消时，再以独立规格评估 gRPC。当前不把“未来可能需要”当成引入运行时依赖的理由。
