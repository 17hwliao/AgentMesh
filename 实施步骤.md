# AgentMesh 实施步骤

> 当前状态：等待 SQL Sentinel 阶段 0 Spike 的可行性结论；可以并行完成接口草图与 mock Provider，但不要过早扩展功能。

> Timebox：全局阶段 1 最多 7 天。超时后保留 API Key、Redis Lua、SSE、取消和两个 CLI；真实 Provider 有接入阻塞时，采用“一个真实 Provider + 可注入故障的 mock Provider”，并在 README 中如实标注，不声称两个真实 Provider 都已接入。

## V0 的 Definition of Done

- 最小 SQL 诊断 CLI（解释给定 SQL/Plan）与文档摘要 CLI 两个客户端均能调用；完整 SQL Sentinel 后续接入；
- Ark/豆包与 Ollama 两个 Provider 可路由；
- 具备 API Key 租户隔离、Redis Lua 配额、SSE、首 Token 前降级；
- 客户端断连后，Context 能取消上游，且无 goroutine 泄漏；
- Trace、Token、耗时、错误、取消原因可查询；
- 所有数据都有真实测试口径。

## 任务拆分

### A. 契约与骨架

- [ ] 创建 Go Module、目录、Makefile、Docker Compose。
- [ ] 定义 `Provider` 接口：非流式、流式、健康检查、usage 解析。
- [ ] 定义 `Router` 接口：根据租户、模型、健康状态选择 Provider。
- [ ] 定义 `QuotaService`、`UsageRecorder`、`StreamRelay` 接口。
- [ ] 定义错误码：认证失败、配额不足、限流、首 Token 前 Provider 失败、流中断。

### B. Provider 与 HTTP 流

- [ ] 实现 Ark Provider Adapter。
- [ ] 实现 Ollama Provider Adapter。
- [ ] 实现 mock Provider：可配置首 Token 延迟、分块数、失败位置。
- [ ] 实现 `/v1/chat/completions` 与 SSE 转发。
- [ ] 将根 Context 传递给 Provider 与所有流式 goroutine。
- [ ] 只在首 Token 前允许 Router 尝试备用 Provider。

### C. 租户、配额、用量

- [ ] 建立 `tenants`、`api_keys`、`model_routes`、`usage_records` 表。
- [ ] 创建 API Key：只保存 Hash 与 Prefix。
- [ ] 实现 Redis Lua 预扣/结算脚本。
- [ ] 实现令牌桶限流。
- [ ] 写入最终 usage；Provider 不支持精确 usage 时标记 estimated。

### D. 可观测与测试

- [ ] Eino Callback 输出结构化 trace。
- [ ] 接入 OTel 与 Prometheus 指标。
- [ ] 测试租户配额隔离。
- [ ] 同一租户 1000 并发预扣测试：配额不透支；两个租户交叉并发时不串扰。
- [ ] 测试首 Token 前降级与首 Token 后失败语义。
- [ ] SSE 客户端断连测试：验证上游取消与 goroutine 回归基线。
- [ ] 使用 mock Provider 进行并发/延迟压测。

## 后置项（V0 不做）

- [ ] 语义缓存；
- [ ] 第三个 Provider；
- [ ] 完整账单与支付；
- [ ] AgentRunner 独立进程与 gRPC；
- [ ] Kubernetes。

## 全局阶段 4：V1 配额一致性专项

> Timebox：最多 7 天。它是 Reservation 状态机和最终一致性问题，不称为 TCC。到期时仍必须保留 reservation 持久表、attempt 痕迹、已转发文本/Token 计量、心跳、幂等 `settle/cancel`、reconciler 与故障注入；usage outbox 聚合和三方完整对账可后置。

### A. 状态与持久化

- [ ] 建立 `quota_reservations`、`provider_attempts` 表；按“先 MySQL `creating` → Redis 预扣 → MySQL `reserved`”落库，reconciler 能补偿两存储之间的中断。
- [ ] 记录每次 attempt 是否已发起；流中按固定间隔更新心跳与“已转发文本 / 本地 tokenizer 计量”。
- [ ] 定义终态只有 `settled`、`cancelled`；`expired_pending` 必须由 reconciler 裁决，不能直接释放。

### B. 结算边界与幂等

- [ ] `cancelled` 仅允许配额校验失败、限流拒绝或确认没有发起 Provider attempt 的本地失败。
- [ ] 任一 Provider attempt 发起后，包括首字节前失败、流中断、客户端取消，必须走 `settled`；只释放确定未使用额度。
- [ ] 每个 fallback attempt 累加 input/已转发 output 计量；下一次调用前检查 reservation 剩余预算。
- [ ] 实现以 `reservation_id + version/request_id` 为幂等键的 `reserve / settle / cancel` Lua 脚本和 MySQL 状态迁移。
- [ ] Provider usage 存在时按其结算；缺失时标记为本地可观测下界，不能写为“精确 token 账单”。

### C. 恢复、账单与故障注入

- [ ] Reconciler 扫描过期 reservation：有 attempt 或已转发计量则 `settled(estimated)`；无 attempt 痕迹才 `cancelled`。
- [ ] 事务写 `usage_outbox`，异步落 `usage_records`；增加 Redis reservation、Provider usage、MySQL usage 的对账任务。
- [ ] mock 故障注入：预扣后进程崩溃、首字节前失败、流中断、客户端取消、重复 settle/cancel、reconciler 重跑。

### D. 验收与退出产物

- [ ] 故障测试证明配额既不永久泄漏，也不重复释放；同一 reservation 最终只产生一次结算结果。
- [ ] `make demo-stage-4`：展示一次 Provider 首字节前失败并 fallback，且两次 attempt 的计量累加；再模拟崩溃后由 reconciler 裁决。
- [ ] 更新顶层 `求职私有资料/resume-bullets.md`、`求职私有资料/interview-faq.md`；公开仓库只更新 `decisions/`。
- [ ] `decisions/` 至少新增“为何不称 TCC”“为何 expired_pending 非终态”“Provider usage 缺失的租户计量策略”。

## 阶段退出产物

- [ ] `make demo-stage-1`：两个 CLI 分别流式调用，演示一次首 Token 前降级和一次客户端取消。
- [ ] 顶层 `求职私有资料/resume-bullets.md`：记录已实测的网关额外延迟、并发配额正确性或取消回收数据；没有实测值不写数字。
- [ ] 顶层 `求职私有资料/interview-faq.md`：至少回答“为何只能首 Token 前切换”“Redis Lua 如何避免额度透支”“断连如何沿 Context 传播”三题；不提交进公开项目仓库。
- [ ] `decisions/`：至少记录 Provider Adapter 边界、配额预扣/结算、是否引入 gRPC 三个决策。
